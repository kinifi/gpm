{
  "name": "gpm-client",
  "version": "0.0.1",
  "description": "the client for a game dev based package manager",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kinifi",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.1.2",
    "progress": "^1.1.8",
    "prompt": "^1.0.0",
    "requestify": "^0.2.5"
  }
}
